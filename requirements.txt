# Torch is installed separately in the Dockerfile with --index-url cu128.
# Do NOT re-pin torch/torchaudio here; let pip keep the cu128 wheels.

fastapi==0.115.6
uvicorn[standard]==0.32.1
websockets==13.1
python-multipart==0.0.12
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.1

# Whisper — all pipelines use faster-whisper (CTranslate2).
faster-whisper==1.2.1
ctranslate2==4.7.1

# Cross-request batching via whisper-s2t (same CT2 backend, list-input API).
# Used by /transcribe-batch endpoint only; /transcribe and /transcribe-fast
# keep using faster-whisper directly.
whisper-s2t==1.3.1

# Diarization — 3.3.x works with huggingface_hub >= 0.23; 4.x requires torchcodec and is a code change.
pyannote.audio==3.3.2
huggingface_hub>=0.23.0,<1.0

# Translation
deep-translator==1.11.4

# Audio stack — numpy <2 is required by numba 0.60 + ctranslate2 4.7 on py3.11.
numpy==1.26.4
scipy>=1.11.0
numba==0.60.0
llvmlite==0.43.0
librosa==0.10.2.post1
soundfile==0.12.1
webrtcvad-wheels==2.0.14
