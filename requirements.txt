# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — voice_model (todos os módulos: STT pipeline + tdvx API)
#
# PyTorch precisa ser instalado ANTES deste arquivo com o index URL correto:
#
#   CPU:  pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#   GPU:  pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# Depois instale o restante:
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── STT core (CTranslate2 backend) ───────────────────────────────────────────
faster-whisper>=1.0.3

# ── Diarização de speakers + embeddings ──────────────────────────────────────
# 3.3+ compatível com torchaudio 2.1+ e huggingface_hub >= 0.22
pyannote.audio>=3.3,<4.0
huggingface_hub>=0.22.0
# speechbrain: forçar upgrade para versão que trata ausência de list_audio_backends
speechbrain>=1.0.2

# ── Operações numéricas ───────────────────────────────────────────────────────
# numpy 2.0 quebra speechbrain internamente — manter <2.0
numpy>=1.24.0,<2.0
scipy>=1.11.0

# ── API Web (tdvx) ────────────────────────────────────────────────────────────
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ── VAD (Voice Activity Detection) ───────────────────────────────────────────
# webrtcvad-wheels: versão com wheels pré-compilados para Windows/Linux
webrtcvad-wheels>=2.0.10

# ── Processamento de áudio ────────────────────────────────────────────────────
soundfile>=0.12.0
librosa>=0.10.1
sounddevice>=0.4.6   # gravação de microfone (record_mic.py / record_mic_live.py)

# ── Utilitários ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
psutil>=5.9.0         # informações de RAM em stt_cpu.py
